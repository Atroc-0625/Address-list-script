name: Build & Release Atroc-0625/Gfwlist2-Ros&CNlist2-Ros&dnsmasq-china-list

on:
  push:
    branches: [ master ]
  schedule:
    - cron: "0 11 */3 * *"
  watch:
    types: [started]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Step 1 - Checkout
        uses: actions/checkout@v2.3.1
      - name: Step 2 - Build Gfwlist2-Ros&CNlist2-Ros&dnsmasq-china-list
        run: sudo bash ./Gfwlist2-Ros.sh && sudo bash ./CNlist2-Ros.sh && sudo bash ./dnsmasq-china-list.sh
      - name: Step 3 - Release Gfwlist2-Ros&CNlist2-Ros&dnsmasq-china-list
        uses: stefanzweifel/git-auto-commit-action@v4.4.0
        with:
          commit_message: Generated by GitHub Actions
